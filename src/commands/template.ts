import { Command } from "commander"; import { AgentTemplateService } from "../services/AgentTemplateService.js"; import { output } from "../utils/output.js";
export function registerTemplateCommand(program: Command, templates: AgentTemplateService): void { const command = program.command("template").description("Manage reusable agent templates"); command.command("list").action(async () => (await templates.list()).forEach((name) => console.log(name))); command.command("info <name>").action(async (name: string) => console.log(JSON.stringify(await templates.info(name), null, 2))); }
