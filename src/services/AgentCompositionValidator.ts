import { access } from "node:fs/promises"; import { join } from "node:path"; import { AgentTemplateService } from "./AgentTemplateService.js";
export class AgentCompositionValidator { public constructor(private readonly templates: AgentTemplateService) {} async validate(name: string) { const manifest = await this.templates.validate(name); const errors: string[] = []; const warnings: string[] = []; const root = this.templates.templatePath(name); await access(join(root, "prompts", "system.md")).catch(() => errors.push("Missing prompts/system.md")); if (!manifest.provider) errors.push("Missing provider configuration."); if (!manifest.capabilities.length) warnings.push("No capabilities declared."); if (manifest.permissions.length === 0) warnings.push("No permissions declared."); return { manifest, errors, warnings, valid: errors.length === 0 }; } }
