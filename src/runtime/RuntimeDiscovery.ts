import { readdir, readFile } from "node:fs/promises"; import { join } from "node:path"; import YAML from "yaml"; import { AgentManifestSchema } from "../services/AgentManifest.js"; import { RuntimeRegistry } from "./RuntimeRegistry.js"; import { WorkflowManager, type Workflow } from "./WorkflowManager.js";
export class RuntimeDiscovery { public constructor(private readonly registry: RuntimeRegistry, private readonly workflows: WorkflowManager) {} async discover(root = process.cwd()): Promise<{ agents: string[]; workflows: string[]; prompts: string[]; tools: string[]; connectors: string[]; knowledge: string[] }> { const agents = join(root, "agents"); const foundAgents: string[] = []; for (const entry of await readdir(agents, { withFileTypes: true }).catch(() => [])) if (entry.isDirectory()) { const manifest = AgentManifestSchema.parse(YAML.parse(await readFile(join(agents, entry.name, "agent.yaml"), "utf8"))); this.registry.registerAgent(manifest.name, manifest); foundAgents.push(manifest.name); } const directory = join(root, "workflows"); const foundWorkflows: string[] = []; for (const entry of await readdir(directory).catch(() => [])) if (entry.endsWith(".yaml")) { const workflow = YAML.parse(await readFile(join(directory, entry), "utf8")) as Workflow; this.workflows.register(workflow); foundWorkflows.push(workflow.id); } const list = async (name: string) => await readdir(join(root, name)).catch(() => []); return { agents: foundAgents, workflows: foundWorkflows, prompts: await list("prompts"), tools: await list("tools"), connectors: await list("connectors"), knowledge: await list("knowledge") }; } }
