import type { Connector } from "./interfaces/Connector.js";
import type { ProviderAdapter } from "./interfaces/ProviderAdapter.js";
import type { Tool } from "./interfaces/Tool.js";
import type { Workflow } from "./WorkflowManager.js";
export class RuntimeRegistry { private readonly providers = new Map<string, ProviderAdapter>(); private readonly tools = new Map<string, Tool>(); private readonly connectors = new Map<string, Connector>(); private readonly workflows = new Map<string, Workflow>(); private readonly agents = new Map<string, unknown>(); registerProvider(item: ProviderAdapter) { this.providers.set(item.name, item); } provider(name: string) { return this.providers.get(name); } registerTool(item: Tool) { this.tools.set(item.name, item); } toolsList() { return [...this.tools.values()]; } registerConnector(item: Connector) { this.connectors.set(item.name, item); } connector(name: string) { return this.connectors.get(name); } registerWorkflow(item: Workflow) { this.workflows.set(item.id, item); } workflow(id: string) { return this.workflows.get(id); } registerAgent(name: string, item: unknown) { this.agents.set(name, item); } agent(name: string) { return this.agents.get(name); } }
